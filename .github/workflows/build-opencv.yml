# Custom OpenCV Android AAR build pipeline
name: Build OpenCV Android AAR

on:
  workflow_dispatch:
    inputs:
      opencv_version:
        description: 'OpenCV git tag to build (e.g. 5.0.0)'
        required: false
        default: '5.0.0'
      release:
        description: 'Create a GitHub Release (tag name = opencv_version)'
        required: false
        default: false
        type: boolean

permissions:
  contents: write   # gh release create/upload

env:
  # OCR pipeline uses only image processing; dnn and its deps (geometry/flann) are not built,
  # inference runs on the custom ONNX Runtime AAR instead
  OPENCV_MODULES: core,imgproc,imgcodecs,videoio,java

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        # JDK 17 (temurin) for the Android Gradle plugin
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          # Python 3.12 for build_sdk.py / build_java_shared_aar.py
          python-version: '3.12'

      - name: Set up Android SDK components
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4.0.1
        with:
          # NDK r27 series; verified working with the OpenCV 5.0 Android build
          packages: 'platforms;android-34 build-tools;34.0.0 ndk;27.3.13750724'

      - name: Prepare environment
        run: |
          # build_sdk.py requires ANDROID_SDK / ANDROID_NDK env vars
          echo "ANDROID_SDK=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_NDK=$ANDROID_HOME/ndk/27.3.13750724" >> "$GITHUB_ENV"
          # AGP 8.6 predates CMake 4.x; remove the SDK copy so AGP falls back to
          # the pinned PATH cmake installed by the Set up CMake step
          rm -rf "$ANDROID_HOME/cmake"

      - name: Install and configure ccache
        run: |
          sudo apt-get install -y ccache
          echo "CCACHE_DIR=$HOME/.cache/ccache" >> "$GITHUB_ENV"
          ccache --max-size=5G

      - name: Restore ccache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ccache
          key: ccache-${{ inputs.opencv_version }}-${{ hashFiles('configs/*.py', '.github/workflows/build-opencv.yml') }}
          restore-keys: |
            ccache-${{ inputs.opencv_version }}-

      - name: Clone OpenCV source
        # shallow clone of the pinned tag v<version>
        run: |
          git clone --depth 1 --branch "$OPENCV_VERSION" https://github.com/opencv/opencv opencv
          # fetch the tag itself so git describe reports "5.0.0" instead of a bare commit
          git -C opencv fetch --depth 1 origin tag "$OPENCV_VERSION"
          echo "OpenCV commit: $(git -C opencv rev-parse HEAD)" >> "$GITHUB_STEP_SUMMARY"
        env:
          OPENCV_VERSION: ${{ inputs.opencv_version }}

      - name: Set up CMake
        # Pin CMake to 3.31.5. build_sdk.py takes cmake from PATH; AGP 8.6 falls
        # back to PATH cmake when the Android SDK has none (see Prepare environment).
        uses: jwlawson/actions-setup-cmake@0d6a7d60b009d01c9e7523be22153ff8f19460d3 # v2.2.0
        with:
          cmake-version: '3.31.5'

      - name: Build native SDK
        # build_sdk.py with configs/opencv.config.py and env.OPENCV_MODULES
        run: |
          # work_dir creates o4a/ (build) and OpenCV-android-sdk/ (result) under it
          python opencv/platforms/android/build_sdk.py build opencv \
            --config "$GITHUB_WORKSPACE/configs/opencv.config.py" \
            --modules_list "$OPENCV_MODULES" \
            --sdk_path "$ANDROID_SDK" \
            --ndk_path "$ANDROID_NDK" \
            --no_samples_build

      - name: Package AAR and Maven repo
        # build_java_shared_aar.py: produces the AAR and the maven repo under build/outputs/
        run: |
          cd build
          # absolute path required: the script copies libs from the SDK and
          # publishes a maven repo under outputs/ (relative to CWD)
          python ../opencv/platforms/android/build_java_shared_aar.py "$GITHUB_WORKSPACE/build/OpenCV-android-sdk" \
            --ndk_location "$ANDROID_NDK" \
            --java_version 17

      - name: Zip Maven repo
        # zip root is org/ (no extra maven_repo/ layer) so it merges with ORT's com/ in maven-local/
        run: |
          cd build/outputs/maven_repo
          python -m zipfile -c ../maven_repo.zip org

      - name: Upload Maven repo
        # archive: false = direct file upload, the download is the zip itself (no wrapping layer)
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: build/outputs/maven_repo.zip
          archive: false

      - name: Upload AAR
        # archive: false = direct file upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: build/outputs/opencv_java_shared_${{ inputs.opencv_version }}.aar
          archive: false

      - name: Create GitHub Release
        # Two channels: dated history tag (one per build date) + version tag (overwrite, consumer contract)
        if: ${{ inputs.release }}
        run: |
          set -e
          cd build/outputs
          AAR=$(find . -name "opencv_java_shared_*.aar" | head -1)
          SHA=$(sha256sum "$AAR" | cut -d' ' -f1)
          COMMIT=$(git rev-parse HEAD)
          cat > release_notes.md <<EOF
          Custom OpenCV $OPENCV_VERSION Android AAR.

          - Build commit: $COMMIT
          - AAR sha256: $SHA
          - Modules: $OPENCV_MODULES
          - ABIs: armeabi-v7a, arm64-v8a, x86, x86_64
          - Shared C++ libraries (c++_shared STL).
          EOF
          # history snapshot (dated tag)
          HIST_TAG="$OPENCV_VERSION-$(date +%Y%m%d)"
          gh release delete "$HIST_TAG" --yes --cleanup-tag 2>/dev/null || true
          gh release create "$HIST_TAG" \
            --target "$GITHUB_SHA" \
            --title "OpenCV $OPENCV_VERSION custom build ($(date +%Y-%m-%d))" \
            --notes-file release_notes.md \
            maven_repo.zip
          # stable GAV channel (overwrite)
          gh release delete "$OPENCV_VERSION" --yes --cleanup-tag 2>/dev/null || true
          gh release create "$OPENCV_VERSION" \
            --target "$GITHUB_SHA" \
            --title "OpenCV $OPENCV_VERSION custom build" \
            --notes-file release_notes.md \
            "$AAR" maven_repo.zip
        env:
          OPENCV_VERSION: ${{ inputs.opencv_version }}
